name: Cypress Tests

on: [push]

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    # Runs tests in parallel with matrix strategy https://docs.cypress.io/guides/guides/parallelization
    # https://docs.github.com/en/actions/using-jobs/using-a-matrix-for-your-jobs
    # Also see warning here https://github.com/cypress-io/github-action#parallel
    strategy:
      fail-fast: false # https://github.com/cypress-io/github-action/issues/48
      matrix:
        containers: [1, 2] # Uses 2 parallel instances
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: user
          POSTGRES_PASSWORD: password
          POSTGRES_DB: library
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Database
        run: |
          npm ci
          # Create .env files
          echo "DATABASE_URL=postgresql://user:password@localhost:5432/library?schema=public" > packages/database/.env
          echo "DATABASE_URL=postgresql://user:password@localhost:5432/library?schema=public" > apps/api/.env
          echo "JWT_ACCESS_SECRET=secure_access_secret" >> apps/api/.env
          echo "JWT_REFRESH_SECRET=secure_refresh_secret" >> apps/api/.env
          echo "NEXT_PUBLIC_API_URL=http://localhost:3002" > apps/web/.env
          
          npx turbo run db:generate
          npx turbo run db:push
        env:
          DATABASE_URL: "postgresql://user:password@localhost:5432/library?schema=public"

      - name: Start Servers
        run: |
          npm run dev &
          echo "Starting servers..."

      - name: Wait for Servers
        run: npx wait-on http://localhost:3002 http://localhost:3003 -t 120000 -v

      - name: Cypress run
        # Uses the official Cypress GitHub action https://github.com/cypress-io/github-action
        uses: cypress-io/github-action@v6
        with:
          install: false
          # Servers already started
          project: ./apps/web
          # Records to Cypress Cloud 
          # https://docs.cypress.io/guides/cloud/projects#Set-up-a-project-to-record
          record: true
          parallel: true # Runs test in parallel using settings above
        env:
          # For recording and parallelization to work you must set your CYPRESS_RECORD_KEY
          # in GitHub repo → Settings → Secrets → Actions
          CYPRESS_RECORD_KEY: ${{ secrets.CYPRESS_RECORD_KEY }}
          # Creating a token https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/creating-a-personal-access-token
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # API & DB Configuration
          DATABASE_URL: "postgresql://user:password@localhost:5432/library?schema=public"
          JWT_ACCESS_SECRET: "secure_access_secret"
          JWT_REFRESH_SECRET: "secure_refresh_secret"
          NEXT_PUBLIC_API_URL: "http://localhost:3002"
